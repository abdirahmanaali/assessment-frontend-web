const colorButton = document.getElementById("color")
const placeButton = document.getElementById("place")
const ritualButton = document.getElementById("ritual")

colorButton.addEventListener("click", () => alert(colorButton.innerHTML) )
placeButton.addEventListener("click", () => alert(placeButton.innerHTML) )
ritualButton.addEventListener("click", () => alert(ritualButton.innerHTML) )
